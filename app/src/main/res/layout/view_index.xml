<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:layout_gravity="end">
    <View
        android:layout_width="@dimen/padding_default"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/padding_default"
        android:layout_marginBottom="@dimen/padding_default"
        android:layout_marginEnd="@dimen/padding_s"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:padding="@dimen/padding_ssss"
        android:layout_gravity="end"
        android:background="@drawable/shape_music_index" />
    <LinearLayout
        android:id="@+id/ll_index"
        android:layout_width="@dimen/size_index"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/padding_default"
        android:layout_marginBottom="@dimen/padding_default"
        android:paddingTop="@dimen/padding_sss"
        android:paddingBottom="@dimen/padding_sss"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:orientation="vertical"
        android:gravity="center_vertical"
        android:layout_gravity="end"/>
</FrameLayout>